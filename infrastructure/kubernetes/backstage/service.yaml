apiVersion: v1
kind: Service
metadata:
  name: backstage
  namespace: backstage
  labels:
    app: backstage
    component: frontend
spec:
  type: NodePort
  ports:
    - port: 7000
      targetPort: 7000
      nodePort: 30950
      protocol: TCP
      name: http
  selector:
    app: backstage
    component: frontend
